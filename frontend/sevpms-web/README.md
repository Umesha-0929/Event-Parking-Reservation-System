# SEVPMS Angular Frontend

This folder is reserved for the Angular application owned by the frontend team.

When the team is ready, create the Angular project inside this folder using the Angular CLI version approved by the team/mentor.

Example from the repository root:

```powershell
cd frontend
ng new sevpms-web --routing --style=scss
```

Do not run this command if the frontend team has already created an Angular project.
