import { Component, ElementRef, HostListener, OnInit, ViewChild, inject, signal } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { ActivatedRoute, Router } from '@angular/router';
import { forkJoin, of, catchError } from 'rxjs';
import { ParkingService } from '../../core/services/parking.service';
import { VenuesService } from '../../core/services/venues.service';
import { EventsService } from '../../core/services/events.service';
import { BookingsService } from '../../core/services/bookings.service';
import { AuthService } from '../../core/services/auth.service';
import { BookingDto, EventDto, ParkingNodeDto, ParkingRecommendationDto, ParkingReservation, ParkingRouteDto, ParkingSlot, ParkingZone, VehicleDto, VenueDto } from '../../core/models/api.models';
import { PageStateComponent } from '../../shared/components/page-state.component';
@Component({selector:'app-parking-page',standalone:true,imports:[FormsModule,PageStateComponent],template:`
<div class="nv-page space-y-6">
<section class="nv-hero nv-photo-hero nv-parking-hero min-h-[220px] text-white"><div class="nv-photo-hero-overlay"></div><div class="relative z-10"><p class="font-extrabold text-sm opacity-75">Parking</p><h1 class="nv-page-title mt-1">Find and reserve parking</h1><p class="mt-2 opacity-80">Choose a venue zone, available slot and saved vehicle. Reservations are linked to a booking.</p></div></section>
<section class="nv-card p-4"><div class="grid md:grid-cols-3 gap-3"><div><label class="nv-label">Venue</label><select class="nv-input" [(ngModel)]="venueId" (ngModelChange)="venueChanged()" aria-label="Venue  id"><option value="">Select venue</option>@for(v of venues();track v.venueId){<option [value]="v.venueId">{{v.name}}</option>}</select></div><div><label class="nv-label">Booking</label><select class="nv-input" [(ngModel)]="bookingId" aria-label="Booking  id"><option value="">Select booking</option>@for(b of eligibleBookings;track b.bookingId){<option [value]="b.bookingId">{{b.bookingNumber}} · {{eventTitle(b.eventId)}}</option>}</select></div><div><label class="nv-label">Saved vehicle</label><select class="nv-input" [(ngModel)]="vehicleId" aria-label="Vehicle  id"><option value="">Enter registration manually</option>@for(v of vehicles();track v.id){<option [value]="v.id">{{v.nickname||v.vehicleType}} · {{v.registrationNo}}</option>}</select></div></div></section>
@if(venueId && nodes().length){<section class="nv-card p-4"><div class="grid lg:grid-cols-[1fr_auto] gap-4 items-end"><div class="grid sm:grid-cols-2 gap-3"><label><span class="nv-label">Parking entrance</span><select class="nv-input" [(ngModel)]="entranceNodeId" aria-label="Entrance node  id"><option value="">Choose entrance</option>@for(n of entranceNodes;track n.id){<option [value]="n.id">{{n.nodeCode}} · {{n.nodeType}}</option>}</select></label><label class="flex items-center gap-3 min-h-[46px] pt-6"><input type="checkbox" [(ngModel)]="requiresAccessible" aria-label="Requires accessible"> <span class="font-bold text-sm">Accessible parking required</span></label></div><button type="button" class="nv-btn nv-btn-primary" [disabled]="recommendBusy()||!entranceNodeId" (click)="recommend()">{{recommendBusy()?'Finding...':'Recommend Best Slot'}}</button></div>@if(recommendation()){<div class="nv-card-soft p-4 mt-4 flex flex-wrap items-center justify-between gap-3"><div><div class="font-black">Recommended: {{recommendation()!.slotCode}}</div><div class="text-sm nv-muted mt-1">{{recommendation()!.reason}}</div></div><button type="button" class="nv-btn nv-btn-secondary" (click)="applyRecommendation()">Select Recommended Slot</button></div>}</section>}
@if(!venueId){<app-page-state title="Choose a venue" message="Select an event venue to view its available parking zones and slots."/>}
@else if(loading()){<div class="nv-skeleton h-96"></div>}
@else if(error()){<app-page-state kind="error" title="Parking unavailable" message="Parking data could not be loaded for this venue." actionLabel="Retry" [action]="retry"/>}
@else{<div class="grid xl:grid-cols-[420px_1fr] gap-5"><section class="space-y-4"><div class="nv-card p-4"><div class="flex items-center justify-between"><div><h2 class="font-black text-lg">Parking zones</h2><p class="nv-muted text-sm">{{zones().length}} zone{{zones().length===1?'':'s'}}</p></div><div class="flex gap-2"><button type="button" class="nv-chip" [class.active]="view==='list'" (click)="view='list'">List</button><button type="button" class="nv-chip" [class.active]="view==='map'" (click)="view='map'">Map</button></div></div><div class="space-y-2 mt-4">@for(z of zones();track z.id){<button type="button" class="w-full text-left nv-card-soft p-4" [style.borderColor]="zoneId===z.id?'var(--focus)':'var(--border)'" (click)="selectZone(z)"><div class="flex justify-between gap-3"><div><div class="font-extrabold">{{z.name}}</div><div class="text-sm nv-muted mt-1">{{z.level||'Level'}} · {{z.entranceName||'Entrance'}}</div></div><span class="nv-status">{{slotCount(z.id)}} slots</span></div></button>}@empty{<p class="nv-muted text-sm">No parking zones are configured for this venue.</p>}</div></div>
@if(selectedSlot()){<div class="nv-card p-5"><h2 class="font-black">Reservation summary</h2><dl class="space-y-3 mt-4 text-sm"><div class="flex justify-between"><dt class="nv-muted">Zone</dt><dd class="font-extrabold">{{selectedZone()?.name}}</dd></div><div class="flex justify-between"><dt class="nv-muted">Slot</dt><dd class="font-extrabold">{{selectedSlot()!.slotCode}}</dd></div><div class="flex justify-between"><dt class="nv-muted">Accessibility</dt><dd class="font-extrabold">{{selectedSlot()!.isAccessible?'Accessible':'Standard'}}</dd></div></dl>@if(!vehicleId){<label class="nv-label mt-4">Vehicle registration</label><input class="nv-input" [(ngModel)]="vehicleRegistration" placeholder="e.g. ABC-1234" aria-label="e.g. ABC-1234">}<button type="button" class="nv-btn nv-btn-primary w-full mt-5" [disabled]="busy()||!bookingId" (click)="reserve()">{{busy()?'Reserving...':'Reserve Parking'}}</button>@if(!bookingId){<p class="text-xs nv-muted mt-2">Complete an event booking before reserving a parking slot.</p>}</div>}</section><section class="nv-card p-4 min-h-[520px]">@if(view==='list'){<div><div class="flex items-center justify-between mb-4"><div><h2 class="font-black text-lg">Available slots</h2><p class="nv-muted text-sm">Choose a slot from the selected zone.</p></div></div><div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 xl:grid-cols-5 gap-3">@for(s of slots();track s.id){<button type="button" class="min-h-24 rounded-2xl border p-3 text-left" [disabled]="!slotAvailable(s)" [style.background]="selectedSlot()?.id===s.id?'color-mix(in srgb,var(--focus) 25%,var(--surface))':'var(--surface-2)'" [style.borderColor]="selectedSlot()?.id===s.id?'var(--focus)':'var(--border)'" (click)="selectedSlot.set(s)"><div class="font-black">{{s.slotCode}}</div><div class="text-xs mt-1" [style.color]="slotAvailable(s)?'var(--success)':'var(--text-2)'">{{s.status}}</div>@if(s.isAccessible){<div class="text-xs mt-2 nv-muted">Accessible</div>}</button>}@empty{<div class="col-span-full"><app-page-state title="No slots in this zone" message="Choose another zone or check again later."/></div>}</div></div>}@else{<div class="h-full"><h2 class="font-black text-lg">Visual lot layout</h2><p class="nv-muted text-sm mt-1">Shows the configured parking-lot layout for this zone.</p><div class="relative h-[440px] mt-4 rounded-2xl overflow-hidden nv-parking-layout border" style="border-color:var(--border)">@for(s of slots();track s.id){<button type="button" class="absolute w-14 h-10 rounded-xl text-[10px] font-black border" [disabled]="!slotAvailable(s)" [style.left.%]="slotX(s)" [style.top.%]="slotY(s)" [style.background]="selectedSlot()?.id===s.id?'#80D5CA':slotAvailable(s)?'var(--surface)':'#444E6D'" [style.color]="selectedSlot()?.id===s.id?'#0F1738':'var(--text)'" [style.borderColor]="selectedSlot()?.id===s.id?'#80D5CA':'var(--border)'" (click)="selectedSlot.set(s)">{{s.slotCode}}</button>}</div></div>}</section></div>}
@if(reservation()){<div class="fixed inset-0 z-[90] bg-black/50 p-4 grid place-items-center"><section #reservationDialog class="nv-card p-6 max-w-lg w-full" role="dialog" aria-modal="true" aria-labelledby="parking-reserved-title"><div class="w-14 h-14 rounded-2xl grid place-items-center text-2xl" style="background:color-mix(in srgb,var(--success) 16%,var(--surface));color:var(--success)">✓</div><h2 id="parking-reserved-title" class="text-2xl font-black mt-4">Parking reserved</h2><p class="nv-muted mt-2">Your parking reservation has been created successfully.</p><dl class="nv-card-soft p-4 mt-5 space-y-2 text-sm"><div class="flex justify-between"><dt>Reservation</dt><dd class="font-extrabold">{{reservation()!.id}}</dd></div><div class="flex justify-between"><dt>Vehicle</dt><dd class="font-extrabold">{{reservation()!.vehicleRegSnapshot}}</dd></div><div class="flex justify-between"><dt>Status</dt><dd class="font-extrabold">{{reservation()!.status}}</dd></div></dl>@if(parkingRoute()){<div class="nv-card-soft p-4 mt-4"><div class="font-black text-sm">Inside parking route</div><p class="nv-muted text-xs mt-1">Follow the configured route from the selected entrance toward your parking area.</p><div class="flex flex-wrap gap-2 mt-3">@for(n of parkingRoute()!.nodes;track n.id){<span class="nv-chip">{{n.nodeCode}}</span>}</div></div>}<div class="flex gap-2 mt-5"><button #reservationClose class="nv-btn nv-btn-primary flex-1" type="button" (click)="closeReservation()">Done</button>@if(selectedVenue()?.latitude&&selectedVenue()?.longitude){<a class="nv-btn nv-btn-secondary flex-1" [href]="mapLink" target="_blank" rel="noopener">Get Directions</a>}</div></section></div>}
@if(message()){<div class="fixed right-4 bottom-24 lg:bottom-4 nv-card px-4 py-3 text-sm" role="status">{{message()}}</div>}
</div>`})
export class ParkingPageComponent implements OnInit{
@ViewChild('reservationDialog') reservationDialog?:ElementRef<HTMLElement>;
@ViewChild('reservationClose') reservationClose?:ElementRef<HTMLButtonElement>;
private reservationReturnFocus:HTMLElement|null=null;
private parking=inject(ParkingService);private venuesApi=inject(VenuesService);private eventsApi=inject(EventsService);private bookingsApi=inject(BookingsService);private auth=inject(AuthService);private route=inject(ActivatedRoute);private router=inject(Router);venues=signal<VenueDto[]>([]);events=signal<EventDto[]>([]);bookings=signal<BookingDto[]>([]);vehicles=signal<VehicleDto[]>([]);zones=signal<ParkingZone[]>([]);nodes=signal<ParkingNodeDto[]>([]);slots=signal<ParkingSlot[]>([]);selectedSlot=signal<ParkingSlot|null>(null);recommendation=signal<ParkingRecommendationDto|null>(null);reservation=signal<ParkingReservation|null>(null);parkingRoute=signal<ParkingRouteDto|null>(null);loading=signal(false);error=signal(false);busy=signal(false);recommendBusy=signal(false);message=signal('');venueId='';eventId='';bookingId='';vehicleId='';vehicleRegistration='';zoneId='';entranceNodeId='';requiresAccessible=false;view:'list'|'map'='list';retry=()=>this.venueChanged();ngOnInit(){this.route.queryParamMap.subscribe(params=>{const event=params.get('event')??'';const venue=params.get('venue')??'';const changed=event!==this.eventId||venue!==this.venueId;this.eventId=event;this.venueId=venue;if(changed&&this.events().length){if(this.eventId&&!this.venueId){const e=this.events().find(x=>x.eventId===this.eventId);if(e)this.venueId=e.venueId;}const b=this.bookings().find(x=>x.eventId===this.eventId&&(x.status===0||x.status===1));this.bookingId=b?.bookingId??'';if(this.venueId)this.venueChanged();}});forkJoin({venues:this.venuesApi.list(),events:this.eventsApi.list({page:1,pageSize:100}),bookings:this.auth.isAuthenticated()?this.bookingsApi.list().pipe(catchError(()=>of([]))):of([]),vehicles:this.auth.isAuthenticated()?this.parking.vehicles().pipe(catchError(()=>of([]))):of([])}).subscribe(v=>{this.venues.set(v.venues);this.events.set(v.events);this.bookings.set(v.bookings);this.vehicles.set(v.vehicles);if(this.eventId&&!this.venueId){const e=v.events.find(x=>x.eventId===this.eventId);if(e)this.venueId=e.venueId;}if(this.eventId){const b=v.bookings.find(x=>x.eventId===this.eventId&&(x.status===0||x.status===1));if(b)this.bookingId=b.bookingId;}const def=v.vehicles.find(x=>x.isDefault);if(def)this.vehicleId=def.id;if(this.venueId)this.venueChanged();});}get selectedZone(){return()=>this.zones().find(z=>z.id===this.zoneId)??null;}get selectedVenue(){return()=>this.venues().find(v=>v.venueId===this.venueId)??null;}get eligibleBookings(){if(!this.venueId)return this.bookings();const eventIds=new Set(this.events().filter(e=>e.venueId===this.venueId).map(e=>e.eventId));return this.bookings().filter(b=>eventIds.has(b.eventId)&&(b.status===0||b.status===1));}eventTitle(id:string){return this.events().find(e=>e.eventId===id)?.title??'Event';}slotCount(zoneId:string){return this.zoneId===zoneId?this.slots().length:'—';}venueChanged(){this.zoneId='';this.slots.set([]);this.nodes.set([]);this.selectedSlot.set(null);this.recommendation.set(null);this.entranceNodeId='';if(!this.venueId)return;this.loading.set(true);this.error.set(false);forkJoin({zones:this.parking.zones(this.venueId),nodes:this.parking.nodes(this.venueId).pipe(catchError(()=>of([]))) }).subscribe({next:v=>{this.zones.set(v.zones);this.nodes.set(v.nodes);const preferred=v.nodes.find(n=>/entrance|gate/i.test(n.nodeType))??v.nodes[0];this.entranceNodeId=preferred?.id??'';this.loading.set(false);if(v.zones.length)this.selectZone(v.zones[0]);},error:()=>{this.error.set(true);this.loading.set(false);}});}selectZone(z:ParkingZone){this.zoneId=z.id;this.selectedSlot.set(null);this.parking.slots(z.id).subscribe({next:s=>this.slots.set(s),error:()=>this.slots.set([])});}slotAvailable(s:ParkingSlot){return /available/i.test(s.status);}slotX(s:ParkingSlot){const xs=this.slots().map(x=>Number(x.x));const max=Math.max(1,...xs);return Math.min(90,Math.max(2,(Number(s.x)/max)*88));}slotY(s:ParkingSlot){const ys=this.slots().map(x=>Number(x.y));const max=Math.max(1,...ys);return Math.min(88,Math.max(2,(Number(s.y)/max)*86));}get entranceNodes(){const candidates=this.nodes().filter(n=>/entrance|gate/i.test(n.nodeType));return candidates.length?candidates:this.nodes();}get selectedEventId(){return this.eventId||this.bookings().find(b=>b.bookingId===this.bookingId)?.eventId||'';}recommend(){if(!this.venueId||!this.entranceNodeId)return;this.recommendBusy.set(true);this.recommendation.set(null);this.parking.recommend({venueId:this.venueId,eventId:this.selectedEventId||null,entranceNodeId:this.entranceNodeId,requiresAccessibleParking:this.requiresAccessible,savedVehicleId:this.vehicleId||null}).subscribe({next:r=>{this.recommendation.set(r);this.recommendBusy.set(false);this.applyRecommendation();},error:e=>{this.recommendBusy.set(false);this.message.set(e?.status===404?'No suitable parking slot is available right now.':e?.error?.error||'A parking recommendation could not be created.');}});}applyRecommendation(){const r=this.recommendation();if(!r)return;const zone=this.zones().find(z=>z.id===r.parkingZoneId);if(!zone)return;this.zoneId=zone.id;this.parking.slots(zone.id).subscribe({next:rows=>{this.slots.set(rows);const slot=rows.find(s=>s.id===r.parkingSlotId);if(slot)this.selectedSlot.set(slot);}});}reserve(){const slot=this.selectedSlot();if(!slot)return;if(!this.auth.isAuthenticated()){void this.router.navigate(['/auth/sign-in'],{queryParams:{returnUrl:this.router.url}});return;}const vehicle=this.vehicles().find(v=>v.id===this.vehicleId);const reg=vehicle?.registrationNo||this.vehicleRegistration.trim();if(!this.bookingId){this.message.set('Choose a booking before reserving parking.');return;}if(!reg){this.message.set('Choose a saved vehicle or enter a registration number.');return;}this.reservationReturnFocus=document.activeElement as HTMLElement|null;this.busy.set(true);this.parking.reserve({bookingId:this.bookingId,parkingSlotId:slot.id,vehicleId:this.vehicleId||null,vehicleRegistration:reg}).subscribe({next:r=>{this.reservation.set(r);this.busy.set(false);this.loadInternalRoute(slot);setTimeout(()=>this.reservationClose?.nativeElement.focus());},error:e=>{this.busy.set(false);this.message.set(e?.error?.message||e?.error?.error||'Parking could not be reserved.');}});}loadInternalRoute(slot:ParkingSlot){this.parkingRoute.set(null);if(!this.venueId||!this.entranceNodeId||!this.nodes().length)return;const candidates=this.nodes().filter(n=>n.id!==this.entranceNodeId);if(!candidates.length)return;const end=candidates.reduce((best,n)=>{const d=(Number(n.x)-Number(slot.x))**2+(Number(n.y)-Number(slot.y))**2;const bd=(Number(best.x)-Number(slot.x))**2+(Number(best.y)-Number(slot.y))**2;return d<bd?n:best;});this.parking.route({venueId:this.venueId,startNodeId:this.entranceNodeId,endNodeId:end.id,accessibleOnly:this.requiresAccessible}).subscribe({next:r=>this.parkingRoute.set(r),error:()=>this.parkingRoute.set(null)});}
closeReservation(){this.reservation.set(null);this.parkingRoute.set(null);const target=this.reservationReturnFocus;this.reservationReturnFocus=null;setTimeout(()=>target?.focus());}
@HostListener('document:keydown',['$event']) onReservationDialogKeydown(event:KeyboardEvent){if(!this.reservation())return;if(event.key==='Escape'){event.preventDefault();this.closeReservation();return;}if(event.key!=='Tab'||!this.reservationDialog)return;const nodes=Array.from(this.reservationDialog.nativeElement.querySelectorAll<HTMLElement>('button:not([disabled]),a[href],input:not([disabled]),select:not([disabled]),textarea:not([disabled]),[tabindex]:not([tabindex="-1"])'));if(!nodes.length)return;const first=nodes[0],last=nodes[nodes.length-1];if(event.shiftKey&&document.activeElement===first){event.preventDefault();last.focus();}else if(!event.shiftKey&&document.activeElement===last){event.preventDefault();first.focus();}}
get mapLink(){const v=this.venues().find(x=>x.venueId===this.venueId);return v?.latitude&&v?.longitude?`https://www.openstreetmap.org/?mlat=${v.latitude}&mlon=${v.longitude}`:'#';}}
