import { Component, OnInit, inject, signal } from '@angular/core';import { DecimalPipe } from '@angular/common';import { RouterLink } from '@angular/router';import { forkJoin, catchError, of } from 'rxjs';import { VenuesService } from '../../core/services/venues.service';import { ReportsService } from '../../core/services/reports.service';import { VenueDto, VenueOwnerReportDto, VenueRentalDto } from '../../core/models/api.models';
@Component({selector:'app-venue-owner-dashboard',standalone:true,imports:[RouterLink,DecimalPipe],template:`<div class="nv-page space-y-6"><div class="flex flex-wrap justify-between items-start gap-3"><div><p class="font-extrabold text-sm nv-muted">Venue Owner</p><h1 class="nv-page-title">Dashboard</h1><p class="nv-muted mt-1">Manage venues, rental requests, parking and venue performance.</p></div><a routerLink="/venue-owner/add-venue" class="nv-btn nv-btn-primary">Add Venue</a></div>@if(report();as r){<div class="grid sm:grid-cols-2 xl:grid-cols-4 gap-4"><article class="nv-card p-5"><div class="nv-muted text-sm">Venues</div><div class="text-3xl font-black mt-2">{{r.venues}}</div></article><article class="nv-card p-5"><div class="nv-muted text-sm">Rental requests</div><div class="text-3xl font-black mt-2">{{r.rentalRequests}}</div></article><article class="nv-card p-5"><div class="nv-muted text-sm">Accepted rentals</div><div class="text-3xl font-black mt-2">{{r.acceptedRentals}}</div></article><article class="nv-card p-5"><div class="nv-muted text-sm">Accepted value</div><div class="text-3xl font-black mt-2">LKR {{r.acceptedRentalValue|number:'1.0-0'}}</div></article></div>}
<div class="grid xl:grid-cols-[1.35fr_.65fr] gap-5"><section class="nv-card p-5 md:p-6"><div class="flex justify-between items-center"><div><h2 class="font-black text-xl">My venues</h2><p class="nv-muted text-sm mt-1">Your venues, availability and operations in one place.</p></div><a routerLink="/venue-owner/venues" class="nv-btn nv-btn-secondary">Manage</a></div><div class="grid md:grid-cols-2 gap-3 mt-5">@for(v of venues();track v.venueId){<a [routerLink]="['/venue-owner/venues',v.venueId,'edit']" class="nv-card-soft p-4"><div class="font-black">{{v.name}}</div><div class="nv-muted text-sm mt-1">{{v.city}} · {{v.capacity}} capacity</div><span class="nv-status mt-3" [class.success]="v.isActive">{{v.isActive?'Active':'Inactive'}}</span></a>}@empty{<div class="md:col-span-2 py-10 text-center nv-muted">No venues yet. Add your first venue to begin.</div>}</div></section><aside class="space-y-4"><a routerLink="/venue-owner/rentals" class="nv-card p-5 block"><div class="font-black">Incoming rentals</div><div class="text-3xl font-black mt-3">{{pendingRentals}}</div><p class="nv-muted text-sm mt-1">Pending organizer requests</p></a><a routerLink="/venue-owner/parking" class="nv-card p-5 block"><div class="font-black">Parking management</div><p class="nv-muted text-sm mt-2">Configure zones and individual slots for your venues.</p></a><a routerLink="/venue-owner/payments" class="nv-card p-5 block"><div class="font-black">Payment QR</div><p class="nv-muted text-sm mt-2">Upload the QR organizers use for venue rental payments.</p></a></aside></div></div>`})
export class VenueOwnerDashboardComponent implements OnInit{private venuesApi=inject(VenuesService);private reports=inject(ReportsService);venues=signal<VenueDto[]>([]);report=signal<VenueOwnerReportDto|null>(null);rentals=signal<VenueRentalDto[]>([]);get pendingRentals(){return this.rentals().filter(x=>Number(x.status)===0).length;}ngOnInit(){forkJoin({venues:this.venuesApi.mine().pipe(catchError(()=>of([]))),rentals:this.venuesApi.rentalsIncoming().pipe(catchError(()=>of([]))),report:this.reports.venueOwner().pipe(catchError(()=>of(null)))}).subscribe(v=>{this.venues.set(v.venues);this.rentals.set(v.rentals);this.report.set(v.report);});}}
